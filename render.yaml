databases:
  - name: stockdb
    databaseName: stockdb
    plan: free

services:
  - type: web
    name: stockchain-backend
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: stockdb
          property: connectionString
      - key: ALLOWED_ORIGINS
        value: https://jporg.in,https://www.jporg.in
      - key: APP_USERNAME
        sync: false
      - key: APP_PASSWORD
        sync: false
      - key: TOKEN_SECRET
        generateValue: true
